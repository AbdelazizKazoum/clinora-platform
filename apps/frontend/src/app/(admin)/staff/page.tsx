import { StaffPage } from '@/features/staff'
import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Staff',
}

const Page = () => {
  return <StaffPage />
}

export default Page
